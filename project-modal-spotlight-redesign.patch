diff --git a/react-src/index.html b/react-src/index.html
index dc2cb5c..e35da5b 100644
--- a/react-src/index.html
+++ b/react-src/index.html
@@ -575,7 +575,6 @@
         <div class="modal-content">
             <div class="modal-header">
                 <div>
-                    <p class="modal-eyebrow">Project Case Study</p>
                     <h3 id="modalTitle" class="modal-title"></h3>
                 </div>
                 <button type="button" class="close-modal" aria-label="Close project details">
diff --git a/react-src/public/css/style.css b/react-src/public/css/style.css
index 669f479..2208e4c 100644
--- a/react-src/public/css/style.css
+++ b/react-src/public/css/style.css
@@ -514,7 +514,6 @@ section h2::after {
 
 .resume-button:focus-visible,
 .academic-certificate-btn:focus-visible,
-.close-modal:focus-visible,
 #visitSiteBtn:focus-visible,
 #visitSourceBtn:focus-visible,
 .rocket-btn:focus-visible,
@@ -1467,8 +1466,12 @@ footer p {
     justify-content: center;
     padding: clamp(12px, 2vw, 26px);
     overflow: hidden;
-    background: rgba(2, 6, 23, 0.78);
-    backdrop-filter: blur(14px);
+    background:
+        radial-gradient(circle at 16% 18%, rgba(124, 58, 237, 0.22), transparent 30rem),
+        radial-gradient(circle at 86% 12%, rgba(6, 182, 212, 0.18), transparent 28rem),
+        rgba(2, 6, 23, 0.82);
+    backdrop-filter: blur(16px);
+    -webkit-backdrop-filter: blur(16px);
 }
 
 .modal.show,
@@ -1478,104 +1481,151 @@ footer p {
 
 .modal-content {
     position: relative;
-    width: min(1180px, 96vw);
-    max-height: min(88vh, 900px);
+    width: min(1120px, 96vw);
+    max-height: min(88vh, 860px);
     display: grid;
     grid-template-rows: auto minmax(0, 1fr) auto;
     padding: 0;
     overflow: hidden;
-    border: 1px solid rgba(255,255,255,0.16);
-    border-radius: 30px;
+    border: 1px solid rgba(148, 163, 184, 0.22);
+    border-radius: 32px;
     color: #e5eefb;
     background:
-        radial-gradient(circle at 18% 8%, rgba(124, 58, 237, 0.18), transparent 24rem),
-        radial-gradient(circle at 88% 8%, rgba(6, 182, 212, 0.16), transparent 24rem),
-        rgba(8, 13, 28, 0.98);
-    box-shadow: 0 34px 100px rgba(0,0,0,0.56);
+        linear-gradient(135deg, rgba(255,255,255,0.08), rgba(255,255,255,0.025)) border-box,
+        linear-gradient(180deg, rgba(10, 15, 31, 0.98), rgba(8, 13, 28, 0.98)) padding-box;
+    box-shadow: 0 34px 100px rgba(0,0,0,0.58);
+}
+
+.modal-content::before {
+    content: "";
+    position: absolute;
+    inset: 0;
+    pointer-events: none;
+    background:
+        linear-gradient(90deg, rgba(124,58,237,0.20), transparent 32%),
+        radial-gradient(circle at 78% 0%, rgba(34,211,238,0.18), transparent 24rem);
+    opacity: 0.9;
+}
+
+.modal-header,
+.modal-scroll-container,
+.modal-actions {
+    position: relative;
+    z-index: 1;
 }
 
 .modal-header {
     display: flex;
-    align-items: flex-start;
+    align-items: center;
     justify-content: space-between;
-    gap: 24px;
-    padding: clamp(20px, 2.4vw, 28px) clamp(22px, 3vw, 34px);
-    border-bottom: 1px solid rgba(255,255,255,0.12);
-    background: linear-gradient(135deg, rgba(124,58,237,0.46), rgba(6,182,212,0.20));
-}
-
-.modal-eyebrow {
-    margin: 0 0 8px;
-    color: #bfdbfe;
-    font-size: 0.78rem;
-    font-weight: 900;
-    letter-spacing: 0.14em;
-    text-transform: uppercase;
+    gap: 22px;
+    padding: clamp(20px, 2.6vw, 34px) clamp(22px, 3vw, 36px) clamp(14px, 1.8vw, 22px);
+    border-bottom: 1px solid rgba(148, 163, 184, 0.16);
 }
 
 #modalTitle,
 .modal-title {
+    position: relative;
     margin: 0;
-    padding: 0;
+    padding: 0 0 0 18px;
     color: #ffffff;
     background: none;
-    font-size: clamp(1.35rem, 2.6vw, 2.2rem);
+    font-size: clamp(1.45rem, 2.5vw, 2.65rem);
     font-weight: 950;
-    line-height: 1.12;
-    letter-spacing: -0.035em;
+    line-height: 1.08;
+    letter-spacing: -0.045em;
+}
+
+#modalTitle::before,
+.modal-title::before {
+    content: "";
+    position: absolute;
+    left: 0;
+    top: 0.16em;
+    bottom: 0.12em;
+    width: 5px;
+    border-radius: 999px;
+    background: linear-gradient(180deg, var(--accent), var(--accent-2));
+    box-shadow: 0 0 26px rgba(34, 211, 238, 0.38);
 }
 
 .modal-scroll-container {
     min-height: 0;
     max-height: none;
     overflow-y: auto;
-    padding: clamp(18px, 2.4vw, 28px);
+    padding: clamp(18px, 2.5vw, 32px) clamp(22px, 3vw, 36px);
     overscroll-behavior: contain;
 }
 
 .modal-layout {
+    position: relative;
     display: grid;
-    grid-template-columns: minmax(280px, 0.82fr) minmax(0, 1.18fr);
-    gap: clamp(18px, 2.4vw, 30px);
-    align-items: start;
+    grid-template-columns: minmax(240px, 0.78fr) minmax(0, 1.22fr);
+    gap: clamp(18px, 2.8vw, 34px);
+    align-items: stretch;
+}
+
+.modal-layout::before {
+    content: "";
+    position: absolute;
+    left: calc(39% + clamp(8px, 1.4vw, 17px));
+    top: 8px;
+    bottom: 8px;
+    width: 1px;
+    background: linear-gradient(180deg, transparent, rgba(103,232,249,0.36), transparent);
+    opacity: 0.85;
 }
 
 .modal-image {
     position: sticky;
     top: 0;
+    align-self: start;
     margin: 0;
-    padding: 12px;
-    border: 1px solid rgba(255,255,255,0.12);
-    border-radius: 26px;
-    background: rgba(255,255,255,0.07);
-    box-shadow: 0 20px 55px rgba(0,0,0,0.35);
+    padding: 10px;
+    border: 1px solid rgba(148, 163, 184, 0.18);
+    border-radius: 28px;
+    background:
+        linear-gradient(145deg, rgba(255,255,255,0.12), rgba(255,255,255,0.04));
+    box-shadow: 0 22px 60px rgba(0,0,0,0.38);
+}
+
+.modal-image::before {
+    content: "";
+    position: absolute;
+    inset: -1px;
+    border-radius: inherit;
+    pointer-events: none;
+    background: linear-gradient(135deg, rgba(124,58,237,0.40), rgba(34,211,238,0.22), transparent 62%);
+    opacity: 0.45;
+    z-index: -1;
 }
 
 .modal-image img {
     width: 100%;
-    aspect-ratio: 16 / 10;
-    max-height: 360px;
+    aspect-ratio: 16 / 11;
+    max-height: 300px;
     object-fit: cover;
     object-position: center;
     display: block;
     margin: 0;
-    border: 1px solid rgba(255,255,255,0.10);
-    border-radius: 18px;
+    border: 1px solid rgba(255,255,255,0.12);
+    border-radius: 20px;
     box-shadow: none;
 }
 
 .modal-description {
     min-height: 100%;
-    padding: clamp(20px, 2.4vw, 28px);
-    border: 1px solid rgba(255,255,255,0.12);
-    border-radius: 26px;
+    padding: clamp(20px, 2.5vw, 32px);
+    border: 1px solid rgba(148, 163, 184, 0.18);
+    border-radius: 28px;
     color: #e2e8f0;
-    background: rgba(15, 23, 42, 0.84);
-    border-color: rgba(148, 163, 184, 0.24);
-    font-size: clamp(0.98rem, 1vw, 1.06rem);
-    font-weight: 500;
+    background:
+        linear-gradient(180deg, rgba(15, 23, 42, 0.94), rgba(15, 23, 42, 0.78));
+    font-size: clamp(1rem, 1.05vw, 1.08rem);
+    font-weight: 560;
     line-height: 1.75;
     text-align: left;
+    box-shadow: inset 0 1px 0 rgba(255,255,255,0.06);
 }
 
 .modal-description p,
@@ -1611,42 +1661,60 @@ footer p {
     display: flex;
     flex-wrap: wrap;
     align-items: center;
-    justify-content: center;
-    gap: 14px;
-    padding: 18px clamp(22px, 3vw, 34px);
-    border-top: 1px solid rgba(255,255,255,0.12);
-    background: rgba(8, 13, 28, 0.92);
+    justify-content: flex-end;
+    gap: 12px;
+    padding: 14px clamp(22px, 3vw, 36px) clamp(16px, 2vw, 22px);
+    border-top: 1px solid rgba(148, 163, 184, 0.16);
+    background: rgba(8, 13, 28, 0.86);
+}
+
+.modal-actions button,
+#myModal .modal-actions button {
+    min-height: 44px;
+    padding: 11px 20px;
 }
 
 #myModal .or-text {
     color: var(--muted);
+    font-size: 0.78rem;
     font-weight: 900;
+    letter-spacing: 0.08em;
+    text-transform: uppercase;
 }
 
 .close-modal {
     flex: 0 0 auto;
-    width: 46px;
-    height: 46px;
+    width: 44px;
+    height: 44px;
     display: grid;
     place-items: center;
     padding: 0;
-    border: 1px solid rgba(255,255,255,0.18);
+    border: 1px solid rgba(255,255,255,0.20);
     border-radius: 50%;
     color: #ffffff;
-    background: rgba(2,6,23,0.58);
+    background: rgba(15, 23, 42, 0.68);
     cursor: pointer;
-    font-size: 1.45rem;
+    font-size: 1.35rem;
     line-height: 1;
     z-index: 3;
+    box-shadow: none;
     transition: transform 180ms ease, background 180ms ease, border-color 180ms ease;
 }
 
 .close-modal:hover {
     transform: scale(1.04);
     border-color: rgba(248, 113, 113, 0.48);
-    background: rgba(239, 68, 68, 0.92);
+    background: rgba(239, 68, 68, 0.88);
+}
+
+.close-modal:focus {
+    outline: none;
 }
 
+.close-modal:focus-visible {
+    outline: 2px solid rgba(103, 232, 249, 0.78);
+    outline-offset: 4px;
+}
 
 @media (max-width: 980px) {
     .modal-content {
@@ -1658,12 +1726,20 @@ footer p {
         grid-template-columns: 1fr;
     }
 
+    .modal-layout::before {
+        display: none;
+    }
+
     .modal-image {
         position: static;
     }
 
     .modal-image img {
-        max-height: 260px;
+        max-height: 240px;
+    }
+
+    .modal-actions {
+        justify-content: center;
     }
 }
 .hidden {
@@ -1844,9 +1920,6 @@ footer p {
         gap: 14px;
     }
 
-    .modal-eyebrow {
-        font-size: 0.7rem;
-    }
 
     .close-modal {
         width: 42px;
